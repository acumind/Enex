name: Deploy to Staging

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - uses: actions/checkout@v4

      # TODO: Build and push images to ACR
      # TODO: Run Alembic migrations against staging DB
      # TODO: Deploy to Azure Container Apps
      # TODO: Run smoke tests
      - name: Placeholder
        run: echo "Staging deployment pipeline — not yet configured"
