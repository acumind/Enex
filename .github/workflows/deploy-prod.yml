name: Deploy to Production

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      # TODO: Manual approval gate
      # TODO: Promote staging image in ACR
      # TODO: Run Alembic migrations against production DB
      # TODO: Deploy to Azure Container Apps
      # TODO: Run smoke tests
      # TODO: Create Sentry release
      - name: Placeholder
        run: echo "Production deployment pipeline — not yet configured"
