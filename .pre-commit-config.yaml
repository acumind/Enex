repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run --directory backend ruff check --fix --config ../backend/pyproject.toml
        language: system
        types: [python]
        files: ^backend/
        pass_filenames: false

      - id: ruff-format
        name: ruff format
        entry: uv run --directory backend ruff format --config ../backend/pyproject.toml
        language: system
        types: [python]
        files: ^backend/
        pass_filenames: false
